# ── Lead Qualification & Outreach Agent — Python dependencies ─────────────────
#
# Pinned to the versions used in development and verified by the eval suite.
# langgraph / langchain-core are NOT listed here: the pipeline is implemented
# as plain Python functions; no LangGraph graph is constructed at runtime.
# If you add graph-based orchestration in a future step, re-add them.

# Data validation and modelling
pydantic==2.13.4
email-validator==2.2.0      # Required for pydantic.EmailStr

# LLM client — optional; app runs in rule-based template mode without a key.
# Set OPENROUTER_API_KEY in .env (or as a Render environment variable) to enable LLM drafting.
openai==2.44.0

# Web UI
streamlit==1.58.0

# Utility
python-dotenv==1.2.2
